{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only the bundled main window may call the typed setup commands.",
  "windows": ["main"],
  "permissions": ["allow-setup-commands"]
}
